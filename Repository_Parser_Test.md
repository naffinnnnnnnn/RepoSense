# Repository Parser 异常与测试设计

## 1. 文档范围

本文整合 Repository Parser 阶段 2 的异常/工程化问题分析与阶段 3 的测试设计。内容按实际解析流程组织，重点说明触发条件、预期行为和关键断言，不展开实现代码。

验证状态：

- **已满足**：当前行为符合预期。
- **已复现缺陷**：测试已稳定暴露当前问题，等待生产实现修复。
- **受阻**：当前架构缺少可注入或可观察边界，暂时无法完成黑盒验证。
- **待补充**：已确认风险，但阶段 3 未增加独立自动化测试。

## 2. CLI 入口与依赖装配

| 问题 | 测试场景与触发 | 预期结果与关键断言 | 类型 | 验证状态 |
|---|---|---|---|---|
| 不同 CLI 调用无法共享解析状态 | 连续执行两次解析，中间创建新 Commit | 第二次读取第一次成功快照，解析范围为增量 | 正常/集成 | 已复现缺陷 |
| CLI 没有事件发布依赖注入点 | 执行一次成功解析并观察事件发布 | 完成事件可被真实或测试发布器观测 | 集成 | 受阻 |
| 非正数超时被延迟为运行期错误 | 分别传入零和负超时 | 参数阶段返回不可重试的输入错误，不访问业务依赖 | 边界 | 已复现缺陷 |
| 多余位置参数被静默忽略 | 合法参数末尾增加未知参数 | 命令立即拒绝，不启动解析 | 异常 | 已复现缺陷 |

## 3. 命令校验与路径 Pattern

| 问题 | 测试场景与触发 | 预期结果与关键断言 | 类型 | 验证状态 |
|---|---|---|---|---|
| TraceID 缺失 | 使用空值和纯空白 TraceID | 返回输入错误；Git、Store 均不被调用 | 边界 | 已复现缺陷 |
| 父目录穿越 Pattern | 输入 `../` 形式的 IncludePath | 返回输入错误，不执行仓库读取 | 安全 | 已满足 |
| Windows 绝对路径绕过校验 | 输入盘符路径和 UNC 路径 | 明确拒绝，Pattern 必须是仓库相对路径 | 安全 | 已复现缺陷 |
| Pattern 分隔符未规范化 | 分别使用正斜杠和反斜杠表达同一范围 | 两种 Pattern 得到完全相同的过滤结果 | 跨平台 | 已复现缺陷 |
| `**` 不具备完整递归语义 | 匹配根目录文件和多层子目录文件 | 零层及任意深度文件均被匹配 | 边界 | 已复现缺陷 |
| 匹配错误可能被当成“不匹配” | 输入语法非法的 Pattern | 校验阶段返回错误，过滤阶段不再忽略匹配异常 | 异常 | 待补充 |

## 4. 幂等处理

| 问题 | 测试场景与触发 | 预期结果与关键断言 | 类型 | 验证状态 |
|---|---|---|---|---|
| 幂等键未绑定完整命令 | 相同键分别使用不同 Ref 或 IncludePaths | 执行新命令或返回明确幂等冲突，禁止静默返回旧结果 | 异常 | 已复现缺陷 |
| 移动 Ref 复用旧 Commit | 第一次解析后移动分支，再用相同键调用 | 不能返回旧 Commit 的缓存结果 | 增量 | 已复现缺陷 |
| 并发幂等查询同时未命中 | 两个请求同时使用相同键 | Git 和 Parser 只执行一次，另一请求等待或复用结果 | 并发 | 已复现缺陷 |
| 幂等命中重复发布完成事件 | 同一成功命令连续调用两次 | 返回相同结果，但完成事件只发布一次 | 副作用 | 已复现缺陷 |
| Context 错误被归为存储故障 | 幂等查询返回取消或超时 | 保留 Context 根因，不标记为持久化失败 | 异常 | 已复现缺陷 |
| 真实 Store 错误信息不足 | 幂等查询返回数据库故障 | 返回持久化错误，并保留阶段、根因和可重试属性 | 异常 | 已满足 |

## 5. Git、仓库身份与增量基线

| 问题 | 测试场景与触发 | 预期结果与关键断言 | 类型 | 验证状态 |
|---|---|---|---|---|
| RepositoryID 被复用于另一仓库路径 | 相同租户和 RepositoryID 依次指向两个仓库 | 在 Diff 前返回不可重试的仓库身份冲突 | 安全/一致性 | 已复现缺陷 |
| 并发增量同步产生分叉 | 两个不同 Commit 同时从同一成功快照开始 | 结果形成线性父子链，不能都以旧快照为父节点 | 并发 | 已复现缺陷 |
| 强推后旧 Commit 消失 | 基线存在，但 Diff 报告旧 Commit 不存在 | 安全降级为全量解析，清空父快照引用 | 恢复 | 已复现缺陷 |
| 仓库不存在被上层错误覆盖 | 对四种 Git 操作使用不存在路径 | 所有操作保留仓库不存在分类 | 异常 | 已复现缺陷 |
| Context 取消被 Git 错误覆盖 | 在各 Git 操作执行期间取消请求 | 返回取消根因，不归类为 Ref 或 Git 业务失败 | 异常 | 已复现缺陷 |
| Git 可执行文件缺失被误报为 Ref 不存在 | 在无 Git 环境解析 Ref | 返回基础设施 Git 故障并保留根因 | 环境 | 已复现缺陷 |
| Diff 基线不存在分类不准确 | 使用已不存在的 Commit 作为基线 | 返回 Ref/Commit 不存在，而不是泛化 Git 故障 | 异常 | 已复现缺陷 |
| Git 输出超过上限 | 输出分别处于上限内和超过上限 | 上限内完整保留；超限明确失败且不可重试 | 资源 | 部分满足：限制有效，分类错误 |
| Copy 和未知 Diff 状态处理不完整 | 输入 Copy 状态和未知状态 | Copy 映射为新增；未知状态明确失败，不能静默跳过 | 协议 | 已复现缺陷 |

## 6. ChangedPath 规范化

| 问题 | 测试场景与触发 | 预期结果与关键断言 | 类型 | 验证状态 |
|---|---|---|---|---|
| 无 Pattern 时复用输入切片 | 修改过滤结果后检查原始变更 | 原始 Git 变更不被反向修改 | 隔离 | 已复现缺陷 |
| 文件上限应用顺序错误风险 | 仓库文件总数超限，但过滤后未超限；以及过滤后仍超限 | 先过滤再限流；确认超限后不读取任何文件 | 资源/边界 | 已满足 |
| ChangedPath 结构未校验 | 输入空路径、未知 Kind、缺少 OldPath 的 Rename | 在读取前返回不可重试的变更校验错误 | 异常 | 已复现缺陷 |
| 重复变更导致重复处理 | 返回两个完全相同的 ChangedPath | 规范化后只保留一条 | 边界 | 已复现缺陷 |
| 相同 Path 缺少次级排序 | 颠倒 Modified 与 Rename 的输入顺序 | 最终 ChangedPaths 顺序完全一致 | 确定性 | 已复现缺陷 |
| Deleted 和 Rename.OldPath 绕过路径检查 | 使用包含 `../` 的删除路径或旧路径 | 在 ReadFile 前拒绝，不保存危险路径 | 安全 | 已复现缺陷 |

## 7. 配置、依赖、时间与 ID

| 问题 | 测试场景与触发 | 预期结果与关键断言 | 类型 | 验证状态 |
|---|---|---|---|---|
| EventPublisher 或 Observer 缺失被静默替换 | 分别缺少两项运行依赖 | 构造阶段明确失败，生产调用必须显式注入 | 配置 | 已复现缺陷 |
| 非正数资源限制被替换为默认值 | MaxFiles、MaxFileBytes 分别传入零和负数 | 构造阶段报告配置错误 | 配置 | 已复现缺陷 |
| ParserVersion 写死 | 使用不同解析器集合执行任务 | Job 记录真实解析器版本摘要 | 一致性 | 待补充 |
| 系统时间回拨可能破坏时间顺序 | Clock 在任务期间返回回拨时间 | 保证 CreatedAt ≤ UpdatedAt ≤ Event 时间 | 边界 | 待补充；正常递增时间已满足 |
| 安全随机源失败终止进程 | 模拟随机源不可用 | 当前任务返回可诊断错误，Worker 不退出 | 环境 | 已复现缺陷 |

## 8. 单文件读取与处理

| 问题 | 测试场景与触发 | 预期结果与关键断言 | 类型 | 验证状态 |
|---|---|---|---|---|
| Gitlink 等非 Blob 被列为文件 | 文件树同时包含 Blob 和 Gitlink | ListFiles 只返回 Blob；ReadFile 明确拒绝非 Blob | Git 对象 | 已复现缺陷 |
| 单文件大小边界 | 文件大小等于上限和超过一字节 | 等于上限可解析；超限记录 too_large 且不调用 Parser | 边界 | 已满足 |
| 二进制识别采样边界错误 | NUL/非法 UTF-8 位于采样边界后，或 UTF-8 字符被截断 | 正确识别二进制，不误判合法文本 | 边界 | 已复现缺陷 |
| 明确二进制内容进入 Parser 的风险 | 输入包含 NUL 的受支持扩展文件 | 记录 binary，Parser 不被调用 | 正常防御 | 已满足 |
| 不支持扩展名被无声忽略 | 输入当前不支持的语言文件 | 记录 unsupported 和 skipped_count，不读取 Blob | 正常防御 | 已满足 |
| Parser 错误未形成失败任务的风险 | Parser 返回确定性错误 | 返回解析失败，保存 FAILED Job、Snapshot 和失败事件 | 异常 | 已满足 |
| 原 Context 取消导致失败状态无法保存 | ReadFile 取消 Context 后返回原始错误 | 使用有界清理上下文保存失败现场，并保留原始 Git 根因 | 恢复 | 已复现缺陷 |

## 9. Parser 与 Code IR 身份

| 问题 | 测试场景与触发 | 预期结果与关键断言 | 类型 | 验证状态 |
|---|---|---|---|---|
| 正则解析无法处理完整结构 | Python 多行声明；TypeScript 块注释包含伪代码 | 合法声明被提取；注释内容不生成 Artifact | 语法 | 已复现缺陷 |
| 重载方法 ArtifactID 冲突 | Java 中定义同名不同参数方法 | 两个方法拥有不同 ArtifactID | 身份 | 已复现缺陷 |
| RelationID 依赖绝对行号 | 只增加非语义空行后重新解析 | ArtifactID、RelationID 保持不变，Evidence 行号可变化 | 稳定性 | 已复现缺陷 |
| 同名符号调用目标歧义 | 不同类包含同名方法，并在类内调用自身方法 | 调用关系指向当前作用域的唯一 ArtifactID | 语义 | 已复现缺陷 |
| 确定语法错误被当成成功 | 输入损坏的受支持源码或配置文件 | Parser 返回错误，不能返回空结果加成功 | 异常 | 已复现缺陷 |
| 等价路径生成不同身份 | 分别使用规范路径和包含 `./` 的等价路径 | ArtifactID 和 SourceRef.Path 完全一致 | 身份 | 已复现缺陷 |

## 10. 结果收口、持久化与事件

| 问题 | 测试场景与触发 | 预期结果与关键断言 | 类型 | 验证状态 |
|---|---|---|---|---|
| 失败进度未包含已检查变更 | 第一个文件被跳过，第二个文件失败 | 失败结果进度为 50%，体现已完成检查项 | 状态 | 已复现缺陷 |
| 成功结果保存失败时返回零值 | 解析完成后让 Store 保存失败 | 返回完整 SUCCEEDED ParseResult 和持久化错误 | 异常 | 已复现缺陷 |
| 结果已保存但事件发布失败 | Broker 首次失败，随后使用相同幂等键重试 | 不重复 Git 解析，只补偿未投递事件 | 恢复 | 已满足 |
| 终态字段或时间不一致 | 使用递增 Clock 完成空仓库解析 | Job、Snapshot、元数据状态一致，时间顺序合法 | 契约 | 已满足 |
| Store 接受矛盾终态 | 分别制造 Job、Snapshot、元数据状态冲突 | 保存被拒绝，不产生可查询结果 | 一致性 | 已复现缺陷 |
| 完成事件不符合 Schema | 分别构造成功和失败事件并校验字段类型 | 必填字段、常量和类型符合版本化 Schema；空数组不是 null | 契约 | 部分满足：失败事件通过，完成事件失败 |
| Graph 消费失败 ParseResult | 使用 FAILED Snapshot 构建 Graph | 在任何写入前拒绝，不产生图修订或事件 | 下游隔离 | 已满足 |
| 完成事件计数与结果不一致风险 | 同时产生 Artifact、Relation 和 SkippedFile | 事件中的 ID、Commit 和数量与 ParseResult 一致 | 契约 | 已满足 |

## 11. 统一失败处理

| 问题 | 测试场景与触发 | 预期结果与关键断言 | 类型 | 验证状态 |
|---|---|---|---|---|
| 早期运行步骤不持久化失败 | ResolveCommit、LatestSnapshot、ListFiles、Diff 分别失败 | 返回分类错误；生成 FAILED Job、Snapshot 和失败事件；幂等查询可找到现场 | 异常 | 已复现缺陷 |
| Retryable 被统一写死 | 分别触发确定性 Parser 错误和临时 Git 读取错误 | Parser 错误不可重试；临时 Git 错误可重试；事件保持一致 | 策略 | 部分满足：临时错误正确，确定错误错误 |
| 二次失败覆盖原始错误 | 原始 Parser 失败后，保存或发布失败状态再次失败 | 错误链同时包含主错误和二次错误，阶段性结果仍为 FAILED | 异常 | 已复现缺陷 |
| 不同错误使用同一泛化文案 | 分别触发 ReadFile 和 Parser 错误 | Code、Operation 和脱敏文案能够区分失败阶段 | 可诊断性 | 已复现缺陷 |
| 失败快照暴露部分 Artifact | 保存带部分 Artifact 的 FAILED ParseResult 后查询 | 标准 Artifact 接口拒绝并返回空结果 | 下游隔离 | 已复现缺陷 |
| 失败幂等记录被当成成功缓存 | 首次临时失败，依赖恢复后使用相同键重试 | 重新执行并产生新成功任务；不可返回失败缓存加 nil | 恢复 | 已复现缺陷 |

## 12. 总体完成标准

Repository Parser 的异常测试达到完成状态时，应满足：

1. 所有已复现缺陷测试转为通过，且不删除或弱化关键断言。
2. 输入、Git、Parser、Store 和事件错误保持准确分类及原始根因。
3. 并发幂等请求只执行一次；不同任务的增量快照保持线性。
4. 成功或失败任务都具有一致、可查询的终态。
5. Snapshot、Job、Code IR、幂等记录和 Outbox 原子保存。
6. 失败或不完整结果不能进入 Graph、RAG、Artifact 等正式下游。
7. 单元测试、并发测试、真实 Git 测试及生产 Store/Event 集成测试共同覆盖上线链路。
